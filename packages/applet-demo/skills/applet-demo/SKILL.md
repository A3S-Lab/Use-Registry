---
name: applet-demo
description: Guidance for the signed Applet demo UI panel and its Tool/MCP binds.
---

# Applet demo

Open the package UI panel after install. The panel depends on the package-local
`echo` Tool Task and `context` MCP server; hosts must project those surfaces in
the same capability generation as the UI document.
